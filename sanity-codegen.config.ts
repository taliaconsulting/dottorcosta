import type { SanityCodegenConfig } from 'sanity-codegen';

const config: SanityCodegenConfig = {
  schemaPath: 'sanity/schemaTypes',
  outputPath: 'sanity/sanity.types.d.ts',
};

export default config;

